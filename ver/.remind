
# pgadmin
admin@local.dev
admin

#Quick add
curl -X POST "http://localhost:8000/notes/" \
  -H "Content-Type: application/json" \
  -d '{"title":"My first note","content":"Learning DevOps!"}'

curl -X PUT "http://localhost:8001/notes/2" \
  -H "Content-Type: application/json" \
  -d '{"title": "Updated Async FastAPI", "content": "Learning CRUD layer is foundation for db manipulation"}'

@app.put("/notes/{id}", response_model=NoteOut)
async def update_note(id: int, data: NoteUpdate, db: AsyncSession = Depends(get_db)):
    note = await crud.update_note_by_id(db, id, data)
    if not note:
        raise HTTPException(status_code=404, detail="Note not found")
    return note

curl -X DELETE "http://localhost:8001/notes/2"

# eter and View Db
docker exec -it devnotes_db psql -U postgres -d devnotes
\dt
\d tableName
